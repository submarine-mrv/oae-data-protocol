# Configuration of generators
# See `just gen-project` task or https://linkml.io/linkml/generators/project-generator.html
---
includes:
  - jsonschema

generator_args:
  jsonschema:
    mergeimports: true
    include_null: false
    title_from: title
    include_range_class_descendants: false

# Unused generators (can be re-enabled as needed):
# excel, owl, markdown, graphql, java, jsonld, jsonldcontext,
# prefixmap, proto, shacl, shex, sqlddl, typescript
